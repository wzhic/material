# Codex 仓库协作规则

本文件适用于整个仓库。目标是让单人维护可持续，而不是让用户充当流程操作员。

## 最小工作流

1. 新会话读取 `SessionStart` 给出的当前任务、分支和允许路径。
2. 当前任务为 `IN_PROGRESS` 时，Codex 可在允许路径内直接创建、编辑、联网查阅、安装常规依赖、测试和返工，不逐项询问。
3. 一个独立功能使用一个任务、一个 `codex/...` 分支和一个 PR；不得夹带第二个功能。
4. 修改前在需求文档或任务摘要中写清目标、范围、验证和回退；保留用户及其他任务的改动。
5. 本地验证必须由 `taskctl run-validation` 或 `run-required` 实际执行，失败、未运行或超时不能记为通过。
6. 提交使用精确文件清单，push 必须非强制；GitHub CI 在 PR 和 `main` 上重新运行验证。
7. 最终合并、部署和发布前停下让用户决定。

## 何时需要用户决定

普通需求中的可逆技术选择、官方脚手架、常规依赖、必要联网、文件编辑、测试、提交、功能分支 push、PR 准备和子 Agent 协作由 Codex 选择合理默认值并直接执行。只有以下事项必须单独确认：

- 跳过或豁免验证；
- 删除历史、强制覆盖、不可逆数据变更等破坏性操作；
- 个人或敏感数据、显著安全风险、实质成本、不兼容迁移；
- 最终合并、部署、发布。

用户给出的清晰需求本身就是范围决定，不再要求重复说“确认”。有会改变结果的歧义时才用人话询问。

## 自动门禁

- `SessionStart` 只负责说明当前任务和下一步。
- `PreToolUse` 阻止任务外写入、受保护机器记录的直接编辑和不安全 Git 操作；不得绕过拒绝。
- `taskctl` 管理任务状态并运行受控验证。
- `reviewctl` 只记录上述关键节点的对话决定，不用于普通编辑审批。
- `reconcile` 核对任务、分支、范围、文档和仓库状态。
- GitHub Actions 展示 CI 结果；Codex 负责核对并报告，普通流程不要求用户提供 Run 链接、令牌、抄写结果或亲自承担日常 CI 核对。

`project-control/tasks/**`、`project-control/reviews/**` 和 `project-control/current-task.json` 只能由控制工具写入。Hook 是 Codex 工具调用边界，不是操作系统安全沙箱。

## 子 Agent 与子任务

- 当前任务可按可独立检查的工作面创建子 Agent 或子任务，不需要逐次请用户确认，也不形成新的产品需求权限。
- 创建前记录父任务、名称、目的、状态和开始时间；回收后记录结束时间与结果摘要。记录由 `taskctl` 维护，迁移期协作记录见 `docs/development/子任务协作-DEV-0002-v1.0.md`。
- 不记录提示词、内部推理、凭据、密钥、原始隐私数据或无关工具日志。
- 子 Agent 继承父任务的允许路径、分支和验证边界；主 Agent 负责核对结果、解决冲突并执行统一验证。

## 文档与设计

- 需求、开发、运维、排障和决策文档放在 `docs/` 对应目录，不混放。
- 每个需求一份版本化主文档，名称以需求名和编号开头。
- 每份需求至少写清：使用者、目标、范围/非范围、流程、数据与安全、异常状态、兼容与回退、验收、验证、假设和未决事项。
- UI 需求必须有用户流程图及入口、取消、空/加载/成功/失败、权限、离线/超时、恢复和无障碍说明。
- 后端、数据和运维需求使用适合的时序图、状态图或数据流图；单张原型图不能替代流程说明。
- 注释只解释原因、不变量、兼容要求和前后置条件；变更历史放在任务、Git 和决策记录。

## 脚手架、依赖与数据

- 初始化应用使用官方生成器，先检查目标目录，不手写依赖清单或伪造脚手架。
- 登录凭据使用系统安全存储，不写入 SQLite、Git、日志或任务记录。
- 个人数据只保存在客户端本地 SQLite；schema 变化需要迁移、备份和回退方案。
- macOS、Windows 客户端共享业务代码，但构建、安装、升级、卸载和回退分别验证；统一后端独立发布。
- 治理 CI 可跨平台验证工具兼容性，但不能替代真实客户端平台验收。

## Git 与完成条件

- 禁止 force push、`--no-verify`、修改测试掩盖失败、降低阈值或删除门禁。
- `LOCAL_VERIFIED` 后普通内容冻结；同范围返工用受控 reopen/recovery，范围变化新建或修订任务。
- 已提交任务可以在 GitHub 等待 CI 时切换到下一个独立任务，不需要导入 Actions 元数据。
- 只有范围内验证完成、GitHub CI 绿色且用户批准最终合并/发布时，才可对外宣称完成或发布。
